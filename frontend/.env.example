# MindVault Environment Variables

# ═══════════════════════════════════════════════════════════════════════════════
# REQUIRED: Sui Network Configuration
# ═══════════════════════════════════════════════════════════════════════════════

# Package ID from deployed contract on testnet
NEXT_PUBLIC_PACKAGE_ID=0xd8e2b3eeeeacbf0f42c0be6c86cc4a95b0a86b884c63678d13fc055afc3d82a6

# ═══════════════════════════════════════════════════════════════════════════════
# OPTIONAL: Walrus Configuration (Testnet defaults are built-in)
# ═══════════════════════════════════════════════════════════════════════════════

# These are the default testnet endpoints - only change if using a different network
# NEXT_PUBLIC_WALRUS_AGGREGATOR=https://aggregator.walrus-testnet.walrus.space
# NEXT_PUBLIC_WALRUS_PUBLISHER=https://publisher.walrus-testnet.walrus.space

# ═══════════════════════════════════════════════════════════════════════════════
# AI Provider Configuration
# ═══════════════════════════════════════════════════════════════════════════════
#
# NOTE: API keys are NOT required as environment variables!
#
# MindVault supports user-configured AI providers in the browser.
# Users enter their own API keys in the Settings page, which are stored
# ONLY in their browser's localStorage (never sent to our servers for storage).
#
# This design gives users full control over:
# - Which AI provider to use (OpenAI, Claude, DeepSeek, Ollama, etc.)
# - Their own API keys (not shared with the platform)
# - Model selection
#
# If you want to provide a default/fallback API key for your deployment,
# you can set it here (but this is optional and not recommended for production):
#
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
